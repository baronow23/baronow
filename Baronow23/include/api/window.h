#pragma once

namespace bapi 
{
//////////////////////////////////////////////////////////////////////////////////////////

	// 현재 커서에 위치하는 윈도우 반환
	BN23_API(CWnd*) GetCursorWindow(void);


	// 시스템 트레이(TrayNotifyWnd) 윈도우의 LeftTop 위치 반환
	BN23_API(bool) GetSystemTrayPosition(_Ret_ CPoint &ptTray);

//////////////////////////////////////////////////////////////////////////////////////////
};